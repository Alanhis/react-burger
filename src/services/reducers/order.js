
const initialState = {
    isRequired: false,
    isRequiredError: false,
    orderDetails: {},
    isOpenModal: false,
}

export const orderReducer = (state = initialState, action) => {
    switch (action.type) {
        default: {
            return state;
        }
    }
}